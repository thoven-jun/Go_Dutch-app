# Dependencies
/node_modules
/backend/node_modules

# Build files
/build
/dist
.cache

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# System files
.DS_Store

# --- 임시 폴더 제외 ---
# 폴더 이름 뒤에 /를 붙이면, 파일이 아닌 폴더 전체를 무시하라는 의미가 됨.
제미나이용 백엔드/
삭제 예정 파일/

# 데이터베이스 파일
db.json

# (선택) SQLite 데이터베이스도 제외
database.sqlite

#.env
.env